#include <iostream>
#include <string>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    string str = "";
    int M = 0;
    cin >> str;
    cin >> M;
    while (M--)
    {
        char c = 0;
        char p = 0;
        cin >> c;
        if (c == 'P') 
            cin >> p;
    }
    string result = "X";
    cout << result;
}